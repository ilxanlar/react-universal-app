import React from 'react';

const styles = {
  alignItems: 'center',
  color: '#999',
  display: 'flex',
  height: '100vh',
  justifyContent: 'center'
};

export default () => (
  <div style={styles}>
    <span>Please wait...</span>
  </div>
);
